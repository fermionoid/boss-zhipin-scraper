# AI 操作手册（给 WorkBuddy / Kimi 等本地 AI Agent 阅读）

你是被用户请来**运行并维护**本工具的 AI 助手。用户不懂技术，请你全程接管，
只在必须由真人操作的两个环节（扫码登录、滑验证码）提醒用户动手。
先完整读完本文件再开始。

## 一、这个工具是什么

通过 CDP（Chrome DevTools Protocol，浏览器远程调试协议）接管用户已手动登录的
浏览器，遍历 Boss直聘网页版「沟通」页（zhipin.com/web/chat/*）左侧会话列表，
逐个点开会话，抓取右侧候选人信息，输出表格和截图。约 300 个会话，
含防风控等待共需 1–2 小时，支持断点续传。

目录结构：

```
1_启动浏览器.bat      # 带 --remote-debugging-port=9222 启动浏览器（独立 profile）
2_开始抓取.bat        # 用 python\python.exe 运行 scraper\main.py
python\               # 免安装 Python 3.11 运行时 + playwright（禁止改动）
scraper\config.py     # ★ 所有 CSS selector、正则、黑名单、参数集中在这里
scraper\main.py       # 主逻辑（一般不要动，见红线）
输出\
  ├─ debug\           # ★ 每次运行自动生成的诊断包（排错主要依据）
  ├─ data.csv         # 最终结果表（utf-8-sig，Excel 直接开）
  ├─ data.md / screenshots\ / raw\
  ├─ log.txt          # 运行日志（含完整 traceback）
  └─ progress.json    # 断点进度（禁止删除）
```

## 二、正常运行流程（用户说"帮我跑抓取"时）

1. 运行 `1_启动浏览器.bat`。会弹出一个浏览器窗口。
2. **提醒用户**：在弹出的浏览器里扫码登录 Boss直聘（此浏览器是独立配置，
   与用户平时的浏览器不共享登录；第二次以后通常免扫码），登录后停在
   「沟通」页面，左上角筛选选「全部」。
3. 用户确认登录好后，运行 `2_开始抓取.bat`，观察黑窗口输出：
   - 「页面还在加载，等待中……」→ 正常，等待即可
   - 「已连接浏览器，开始抓取」→ 成功启动
   - 逐条出现「已抓取：某某」→ 一切正常，让它跑完（1–2 小时）
   - 「检测到登录失效或安全验证」→ 提醒用户在浏览器里扫码或滑验证码，
     程序每 15 秒自查，处理完自动继续
4. 结束后向用户汇报黑窗口最后的统计（成功/跳过/失败数），并指给用户
   `输出\data.csv`。
5. 中断无害：任何时候重跑 `2_开始抓取.bat` 都会自动续传，不会重复抓。

## 三、出错时的修复流程（按顺序）

1. **读诊断材料**：`输出\log.txt` 最后一段报错 + `输出\debug\` 里**时间最新**的
   `*_report.txt`。报告的「selector 体检」表列出 config.py 每条 selector 在
   真实页面上的命中数与首个元素文本——命中为 0 的组即失配点。
   同目录 `*_page.html` 是当时的真实页面 DOM，可搜索类名确定正确 selector。
2. **只改 `scraper\config.py`**。改前先复制一份为 `config.py.bak`。
3. **语法自检**：`python\python.exe -m py_compile scraper\config.py`，无输出即通过。
4. **验证**：重跑 `2_开始抓取.bat`，出现「已抓取：某某」即修复成功；
   再出错则回到第 1 步看新生成的 debug 报告。

### 常见故障对照表

| 现象 | 原因 | 修法 |
|---|---|---|
| 无法定位会话列表（时间戳校验） | conversation_list selector 失配或页面未加载完 | 对照 debug 报告和 page.html 修 `SELECTORS["conversation_list"]` |
| 左侧会话列表为空 | conversation_item selector 失配 | 修 `SELECTORS["conversation_item"]`（`:scope ` 前缀=只在容器内找） |
| CSV 某列大面积空白 | 该字段 selector 失配 | 按 `FIELD_SELECTOR_MAP` 找到对应组修；正则兜底在 `REGEX_PATTERNS` |
| 大量「失败，已跳过」 | 渲染等待不足或姓名校验 | 调大 `RENDER_TIMEOUT_SECONDS`；无效则收集 debug 包反馈交付人员 |
| 点到菜单/页面跳走 | 导航元素混入列表 | 向 `NAV_BLOCKLIST` 增加对应名称 |
| 「检测到安全验证」误报（实际没验证码） | `SECURITY_KEYWORDS` 某词过宽 | 删掉过宽关键词；禁止加入"验证"这类裸词 |
| 需要调节奏 | 防风控参数 | `WAIT_MIN/WAIT_MAX/LONG_BREAK_*`，只准往慢调 |

## 四、红线（任何情况下都要遵守）

1. **禁止直接操作 Boss直聘网页**：不要在页面里点击、翻页、发消息、下载。
   这是用户的招聘账号，异常操作会触发平台风控甚至封号。浏览器里的一切
   人工操作（扫码、滑验证码）只由用户本人完成，你只负责运行 bat 和改配置。
2. **禁止修改 `main.py`**（除非上表明确指向），禁止重构、禁止顺手优化。
3. **禁止改动** `python\` 目录、两个 `.bat`、`progress.json`、`browser_profile\`。
4. 禁止安装依赖、联网下载——运行时全部自带。
5. 改动最小化：一次只改与故障直接相关的条目，改完立即验证。
6. 修不好、或问题指向 main.py 时，如实告诉用户：
   「请把 输出\debug 文件夹和 log.txt 打包发给交付人员」。不要硬改。
7. `输出\` 下的候选人数据属于用户的招聘业务数据：除本文件夹外不要复制、
   上传或发送到任何地方。

## 五、用户提示语模板

运行：
> 「请读这个文件夹里的 AI操作手册.md，帮我运行抓取工具。」

修复：
> 「抓取工具报错了。请读 AI操作手册.md，根据 输出\debug 最新报告修复，
> 改完带我验证。」
