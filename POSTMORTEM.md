# 杀掉标签页的那只手

一位客户的招聘账号里堆了六百多个候选人的沟通消息，想导成表格——都是投递到她自己岗位的人，属于她本人的业务数据。我判断这是个小活：用程序接管她已经登录好的浏览器，逐个点开、读文字、写 CSV。不自己起浏览器、不碰登录，在我的经验里这就是绕开风控最稳的做法。

程序连上了，也读到了候选人列表。但每一个人都抓不下来。

接下来我修了三个 bug，每个都是真的：收集逻辑逐字段查 DOM，四十个人要跑四十秒，等它跑完页面早变了；自动关弹窗的代码误点到平台的推广按钮，把页面点跳走了；程序退出时的清理流程会顺手关掉用户的浏览器。三个都修好了，页面还是会莫名其妙消失。

## 停手，做一个对照实验

第三次说"我找到元凶了"却还没好，我才意识到自己一直在错误的层面上修东西。于是不再猜：接管前用纯 HTTP 观察十五秒，页面活得好好的；接管后再观察十五秒，页面两秒内被销毁。

> 平台检测的不是我的动作，是"浏览器被调试器接管"这件事本身。只要走这条路，第四个 bug 修好了还会有第五个。

换成浏览器书签之后，第二次就跑通了。所谓书签，就是把一段 `javascript:` 开头的代码存进收藏夹，点一下在当前页面里执行——跟图书馆那种"校外访问"书签是同一个东西，只是脚本长一些。它不需要调试端口，也就不存在那个可被检测的信号。

| | 调试器接管 | 浏览器书签 |
|---|---|---|
| 代码在哪运行 | 浏览器外部 | 页面内部，是网页自己的脚本 |
| 平台能否识别 | 能 | 与手动点击无法区分 |
| 依赖 | 调试端口、独立配置、运行时 | 无 |
| 代价 | — | 拿不到整页截图 |

## 换了机制，旧账还得还一次

书签版跑通的第二天，账号被限速了。原因不在新机制：Python 版本来有"每三十人长休两到五分钟"的节奏，我移植成书签时只保留了每人一两秒的短等待，长休漏掉了。六百个人连续不断地点，被抓是必然的。

补的时候我没有直接填一个更保守的数字——那还是在猜。改成让它自己退：连续失败就把间隔翻倍并立刻长休，连续成功再慢慢恢复，同时把当前节奏显示在面板上。速度档位仍然可以手动选，但不需要谁一开始就猜对。

## 我改了什么判断

**"第三次还没修好"是个可执行的信号。** 以前我把"又找到一个 bug"当成进展。现在我把它当成计数器：同一现象修到第三轮，说明诊断层级错了，该停手去区分"我的代码"和"环境"，而不是修第四个。

**对照实验比补丁便宜得多。** 那个"接管前 vs 接管后"的实验只花三十秒，却是唯一给出定论的一步。它本该是第一个动作，我却把它排在了三轮补丁后面。

**换机制常常比修实现快。** 换掉机制之后，之前所有的坑——独立浏览器配置、调试端口、免安装运行时、自动更新通道——一次性全部消失了。它们本来就是那条路带来的，不是问题本身。

**移植时最容易丢的是"看不见的机制"。** 字段解析、点击、导出这些看得见的东西我都搬过去了，唯独漏了节流。它平时不产生任何输出，只在缺席时才被平台告知。换实现时该专门清点一遍这类东西。

**参数拿不准，就别填数字，让它自己退。** 被限速之后我本可以把间隔从两秒改成六秒，但那个六秒同样是猜的。让程序按失败信号自动退让、按成功慢慢恢复，它会贴合这个站点当下的容忍度。

**远程交给不懂技术的人用，工具得自己会说话。** 版本号显示在屏幕上、程序自己打印错在哪、客户端能自动拉最新代码。少一样，排查就退化成隔着微信传文件。我们有一轮完全白试，就是因为对方书签没更新成功，而双方都看不出来。
